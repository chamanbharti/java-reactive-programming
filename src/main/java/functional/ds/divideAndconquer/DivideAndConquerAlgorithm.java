package functional.ds.divideAndconquer;
/*
Divide - original problem is divided into sub problems
Conquer - Solve each sub-problem one at a time, recursively
Combine - Put the solutions to the sub-problems together to get the solution to the whole problem

                                     Problem
                                    || split/merge               ||
       Sub problem          Sub problem                         Sub problem
          ||    split/merge   ||                         ||    split/merge   ||
       Compute subproblem   Compute subproblem        Compute subproblem   Compute subproblem
 */
public class DivideAndConquerAlgorithm {
}
